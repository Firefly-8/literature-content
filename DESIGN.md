# 📚 学霸小助手 (Study Helper) - MVP 方案设计文档

> K12 语文数学英语学习辅助微信小程序
> 版本：v1.0 MVP
> 日期：2026-08-04
> 作者：小克

---

## 一、技术栈选型

### 1.1 前端：原生微信小程序

| 选项 | 结论 | 理由 |
|------|------|------|
| 原生小程序 (WXML + WXSS + JS) | ✅ **选用** | MVP 最快路径；无需编译构建；微信 IDE 直接调试上传；性能最好；审核最稳 |
| uni-app | ❌ | 多端复用是伪需求（只做微信）；引入额外编译层 |
| Taro | ❌ | React 写法对小程序不友好；学习成本高 |

**UI 框架**：WeUI 2.x（微信官方组件库，风格和微信一致，审核友好）

### 1.2 后端：微信云开发 (Cloud Development)

| 选项 | 结论 | 理由 |
|------|------|------|
| 微信云开发 | ✅ **选用** | 免服务器、免运维；自带数据库+存储+云函数；免费额度够 MVP 用；内置微信登录 |
| Node.js (Express/Koa) 自建 | ❌ | 需要服务器、域名、HTTPS、运维成本 |
| Python (Flask/FastAPI) 自建 | ❌ | 同上 |

**云开发核心能力使用**：
- **云数据库**：MongoDB-like 文档数据库，存所有业务数据
- **云函数**：Node.js 服务端逻辑（AI 调用、业务处理）
- **云存储**：暂用不到（题目纯文本）
- **内置登录**：`wx-server-sdk` 自动获取 openid，无需自己实现 OAuth

### 1.3 数据库：云数据库 (Cloud DB)

- 微信云开发自带的文档型数据库
- 类似 MongoDB 的 JSON 结构
- 前端可直接调用（有安全规则），敏感操作走云函数

### 1.4 AI 调用：MiniMax API

| 选项 | 结论 | 理由 |
|------|------|------|
| MiniMax | ✅ **选用** | 已有 key；中文能力强；价格低（作文批改场景成本可控） |
| LongCat | ❌ | 自有模型，不确定外部调用成本 |
| DashScope (通义千问) | 备选 | 中文能力强，但价格稍高 |

**AI 使用场景**：
1. 作文批改（核心）：接收学生作文 → 返回评分+纠错+建议
2. 题目生成（辅助）：批量生成题库（离线一次性）

**调用方式**：前端 → 云函数 → MiniMax API（API Key 存在云函数环境变量，不暴露到前端）

---

## 二、系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                      微信小程序 (前端)                            │
│                                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ 题库练习  │  │ 错题本   │  │ 背诵小屋  │  │ 作文批改  │        │
│  │ Practice │  │ WrongBook│  │ Memory   │  │ Compose  │        │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘        │
│       │              │              │              │              │
│  ┌────┴──────────────┴──────────────┴──────────────┴────┐        │
│  │              页面路由 + 状态管理 (App.globalData)      │        │
│  └────┬──────────────┬──────────────┬──────────────┬────┘        │
└───────┼──────────────┼──────────────┼──────────────┼─────────────┘
        │              │              │              │
   ┌────▼──────────────▼──────────────▼──────────────▼────┐
   │              wx-server-sdk (客户端 SDK)               │
   │         · 微信登录 (wx.login → openid)                │
   │         · 云数据库直读 (安全规则控制)                   │
   │         · 云函数调用 (wx.cloud.callFunction)           │
   └────┬──────────────┬──────────────┬──────────────┬────┘
        │              │              │              │
┌───────▼──────────────▼──────────────▼──────────────▼─────────────┐
│                       微信云开发 (后端)                            │
│                                                                   │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐   │
│  │   云数据库       │  │    云函数        │  │   云存储         │   │
│  │  (Cloud DB)     │  │ (Cloud Functions)│  │ (Cloud Storage) │   │
│  │                 │  │                 │  │                 │   │
│  │ · users         │  │ · getOpenId     │  │ · (预留)        │   │
│  │ · children      │  │ · correctEssay  │  │                 │   │
│  │ · questions     │  │ · genQuestions  │  │                 │   │
│  │ · wrongQuestions│  │ · getReviewList │  │                 │   │
│  │ · memorizeItems │  │                 │  │                 │   │
│  │ · memorizeProg  │  │                 │  │                 │   │
│  │ · compositions  │  │                 │  │                 │   │
│  └─────────────────┘  └────────┬────────┘  └─────────────────┘   │
│                                │                                  │
│                                │ HTTPS                           │
│                                ▼                                  │
│                     ┌─────────────────┐                          │
│                     │  MiniMax API    │                          │
│                     │  (LLM 服务)     │                          │
│                     └─────────────────┘                          │
└───────────────────────────────────────────────────────────────────┘
```

### 数据流说明

```
1. 登录流程：
   用户打开小程序 → wx.login(code) → 云函数 getOpenId → 返回 openid → 查 users 表 → 绑定身份

2. 刷题流程：
   选择年级科目 → 前端读 questions 集合 → 展示题目 → 用户作答 → 判分
   → 错题写入 wrongQuestions 集合 → 计算下次复习时间

3. 错题复习流程：
   进入错题本 → 云函数 getReviewList → 查询 next_review_time <= 今天 的错题 → 推送复习

4. 作文批改流程：
   输入作文 → 云函数 correctEssay → 调 MiniMax → 返回批改结果 → 存入 compositions

5. 背诵流程：
   选择类型(单词/古诗/公式) → 读 memorizeItems → 展示 → 标记掌握/未掌握
   → 更新 memorizeProgress → 按遗忘曲线安排下次出现
```

---

## 三、数据模型

### 3.1 users (用户表 / 家长)

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | string | 自动生成的文档 ID |
| openid | string | 微信 openid（唯一标识） |
| nickname | string | 微信昵称 |
| avatarUrl | string | 头像 URL |
| createdAt | date | 创建时间 |

> 云开发会自动在插入时注入 `_openid`，无需手动存 openid，但查询时需要

### 3.2 children (孩子档案表)

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | string | 文档 ID |
| parentId | string | 关联 users._id |
| name | string | 孩子昵称 |
| grade | string | 年级：一年级~高三（共12级） |
| avatar | string | 头像（默认提供几个卡通头像可选） |
| subjects | array | 关注的科目 ["语文","数学","英语"] |
| createdAt | date | 创建时间 |

### 3.3 questions (题库表)

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | string | 文档 ID |
| subject | string | 科目：语文/数学/英语 |
| grade | string | 年级 |
| type | string | 题型：选择题/填空题/判断题/计算题/应用题 |
| difficulty | number | 难度 1-3（易/中/难） |
| content | string | 题目内容 |
| options | array | 选择题选项 [{key:"A", value:"xxx"}, ...] |
| answer | string | 正确答案 |
| explanation | string | 答案解析 |
| tags | array | 知识点标签 ["四则运算","古诗词填空"] |
| createdAt | date | 创建时间 |

### 3.4 wrongQuestions (错题表)

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | string | 文档 ID |
| childId | string | 关联 children._id |
| questionId | string | 关联 questions._id |
| myAnswer | string | 用户选的错误答案 |
| wrongCount | number | 累计错误次数 |
| lastWrongTime | date | 最近一次答错时间 |
| nextReviewTime | date | 下次复习时间（根据遗忘曲线计算） |
| mastery | number | 掌握度 0-100（连续答对增加） |
| reviewHistory | array | 复习记录 [{date, correct:bool}] |

### 3.5 memorizeItems (背诵内容表)

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | string | 文档 ID |
| type | string | 类型：单词/古诗/公式/生字 |
| subject | string | 科目：语文/数学/英语 |
| grade | string | 年级 |
| title | string | 标题/单词/公式名 |
| content | string | 内容（英文单词含释义、古诗全文、公式表达式） |
| extra | object | 额外信息 {phonetic:"", translation:"", example:""} |
| difficulty | number | 难度 1-3 |

### 3.6 memorizeProgress (背诵进度表)

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | string | 文档 ID |
| childId | string | 关联 children._id |
| itemId | string | 关联 memorizeItems._id |
| status | string | 状态：learning/mastered/reviewing |
| reviewCount | number | 复习次数 |
| lastReviewTime | date | 最近复习时间 |
| nextReviewTime | date | 下次复习时间 |
| correctStreak | number | 连续正确次数 |

### 3.7 compositions (作文表)

| 字段 | 类型 | 说明 |
|------|------|------|
| _id | string | 文档 ID |
| childId | string | 关联 children._id |
| title | string | 作文题目 |
| content | string | 作文正文 |
| wordCount | number | 字数 |
| score | number | AI 评分 0-100 |
| feedback | object | AI 反馈 {overall:"", strengths:[], improvements:[], errors:[]} |
| createdAt | date | 提交时间 |

---

## 四、功能优先级

### 🔴 MVP 必做（跑通核心闭环）

| 功能 | 子功能 | 说明 |
|------|--------|------|
| **A. 题库练习** | 选择题刷题 | 年级/科目筛选 → 展示题目 → 作答 → 即时判分 → 看解析 |
| | 填空题/判断题 | 同上逻辑，适配不同题型 |
| | 答题统计 | 正确率、用时 |
| **B. 错题本** | 自动归集 | 答错自动加入错题本 |
| | 错题重做 | 错题列表 → 重新作答 → 答对则降低复习频率 |
| | 简化遗忘曲线 | 1天/2天/4天/7天/15天 五轮复习提醒 |

### 🟡 MVP 应该做（增强体验）

| 功能 | 子功能 | 说明 |
|------|--------|------|
| **C. 背诵小屋** | 单词背诵 | 英语单词 + 释义 + 例句，标记认识/不认识 |
| | 古诗背诵 | 古诗全文 + 默写填空模式 |
| | 公式背诵 | 数学公式卡片式学习 |
| **账户体系** | 微信登录 | 云开发自动获取 openid |
| | 多孩子档案 | 家长可添加多个孩子，切换使用 |
| | 年级设置 | 不同年级展示不同内容 |

### 🟠 后续迭代（V2）

| 功能 | 说明 |
|------|------|
| **D. 作文批改** | LLM 打分 + 纠错 + 建议（MiniMax 调用） |
| 学习报告 | 周报/月报生成，图表展示进步 |
| 家长监督 | 家长查看孩子学习数据 |
| 题目收藏 | 收藏好题 |
| 口语评测 | 语音识别 + 发音评分（技术复杂度高，V3） |

### 🔴 砍掉/不做

| 功能 | 理由 |
|------|------|
| 社交/排行榜 | MVP 不需要，且微信审核对社交功能审查严 |
| 直播/视频 | 完全不需要 |
| 支付/会员 | MVP 全免费 |
| 拍照搜题 | 需要 OCR + 大模型，成本高 |

---

## 五、题目来源方案

### 方案：AI 生成 + 人工审核（推荐）

| 方案 | 优劣 | 结论 |
|------|------|------|
| 自己手写 100+ 题 | 质量可控但耗时巨大 | ❌ |
| 网上爬现成题库 | 版权风险大，微信审核可能不通过 | ❌ |
| **AI 批量生成** | 快、可定制、无版权问题（AI 生成内容） | ✅ |

### 具体执行

1. **用 MiniMax/LongCat 批量生成题目**
   - 按科目 × 年级 × 题型生成
   - 每次生成 10 题，调用 10 次凑 100+ 题
   - Prompt 模板：
     ```
     请生成 10 道小学三年级数学选择题，要求：
     - 考察两位数×一位数
     - 4个选项，含1个正确答案
     - 附详细解析
     - 输出 JSON 格式
     ```

2. **题目分布规划**

| 科目 | 年级范围 | 题量 | 题型 |
|------|----------|------|------|
| 语文 | 小一至高三 | 40 题 | 选择(生字/拼音/成语)、填空(古诗词)、判断 |
| 数学 | 小一至高三 | 40 题 | 选择、填空、计算、应用 |
| 英语 | 小一至高三 | 30 题 | 选择(词汇/语法)、填空(单词拼写) |
| **合计** | | **110 题** | |

3. **生成后处理**
   - 格式校验（JSON 解析检查）
   - 答案合理性抽检（随机抽 20% 人工看一眼）
   - 去重（检查重复题目）
   - 导入云数据库（写个云函数批量导入）

---

## 六、开发计划

### 阶段一：基础框架搭建（预计 1-2 天）

| 任务 | 交付物 |
|------|--------|
| 注册微信小程序账号 | AppID |
| 开通云开发 | 环境 ID |
| 项目初始化 | 可运行的空壳小程序 |
| 配置 WeUI | UI 框架就绪 |
| 云数据库建集 | 6 个集合创建 |
| 登录流程跑通 | 能获取 openid，创建用户记录 |

### 阶段二：题库练习功能（预计 2-3 天）

| 任务 | 交付物 |
|------|--------|
| AI 生成 110+ 题 | JSON 题库文件 |
| 题目导入云函数 | 数据库有数据 |
| 答题页面 UI | 选择题/填空题/判断题组件 |
| 答题逻辑 | 作答 → 判分 → 展示解析 |
| 年级/科目筛选 | 可按条件选题 |

### 阶段三：错题本功能（预计 1-2 天）

| 任务 | 交付物 |
|------|--------|
| 错题自动归集 | 答错自动写入 |
| 错题列表页 | 按科目/时间排序 |
| 错题重做 | 重新作答逻辑 |
| 遗忘曲线算法 | 1/2/4/7/15 天计算 |
| 复习提醒 | 今日待复习错题列表 |

### 阶段四：账户体系 + 背诵功能（预计 1-2 天）

| 任务 | 交付物 |
|------|--------|
| 多孩子档案管理 | 添加/切换孩子 |
| 单词背诵模块 | 卡片式学习 |
| 古诗背诵模块 | 默写填空模式 |
| 公式背诵模块 | 卡片式学习 |
| 背诵进度追踪 | 掌握/未掌握标记 |

### 阶段五：整合测试 + 发布（预计 1 天）

| 任务 | 交付物 |
|------|--------|
| 全流程测试 | 登录→选孩子→刷题→看错题→背诵 |
| 真机调试 | 微信开发者工具 + 手机预览 |
| 代码上传 | 上传为体验版 |
| 提交审核 | 提交微信审核 |

### 总工作量估算

| 阶段 | 天数 |
|------|------|
| 阶段一：基础框架 | 1-2 天 |
| 阶段二：题库练习 | 2-3 天 |
| 阶段三：错题本 | 1-2 天 |
| 阶段四：账户+背诵 | 1-2 天 |
| 阶段五：测试发布 | 1 天 |
| **总计** | **6-10 天** |

> 如果只追求"能跑"，阶段一+二+五可在 4-5 天内交付最小可用版本。

---

## 七、风险点与应对

### 7.1 微信审核风险 ⚠️

| 风险 | 说明 | 应对 |
|------|------|------|
| 教育类资质 | 微信对"教育"类目审核严格，可能需要 ICP/网文证 | MVP 定位为"学习辅助工具"而非"在线教育"，规避教育类目敏感词 |
| 内容合规 | 题目内容不能涉政/涉黄/敏感 | 题目严格限定在学科知识 |
| 用户数据 | 涉及未成年人数据需谨慎 | 只收集最少信息（昵称、年级），不收集手机号 |

### 7.2 数据存储成本 💰

| 风险 | 说明 | 应对 |
|------|------|------|
| 云开发免费额度 | 免费额度：数据库读 200万次/月，写 100万次/月，云函数 200万次/月 | MVP 阶段 2-3 个家庭用完全够；超出后按量付费也很便宜 |
| 题目存储 | 110 题文本数据极小 | 可忽略 |

### 7.3 LLM 调用成本 💰

| 风险 | 说明 | 应对 |
|------|------|------|
| 作文批改调用 | MiniMax 按 token 计费 | MVP 阶段作文批改不是核心功能；如果做，限制每次批改字数上限（如 800 字） |
| 题目生成 | 一次性生成，成本极低 | 约 1-2 元即可生成 100+ 题 |

### 7.4 题目质量风险 📝

| 风险 | 说明 | 应对 |
|------|------|------|
| AI 生成题目有错误 | 数学答案可能算错、语文可能有错别字 | 生成后抽检 20%，错误率可控在 5% 以内；后续迭代修正 |
| 年级匹配不准 | AI 可能生成超纲题 | Prompt 中明确年级和知识点范围 |

### 7.5 技术风险 🔧

| 风险 | 说明 | 应对 |
|------|------|------|
| 云函数冷启动 | 首次调用可能有 1-2 秒延迟 | 加 loading 动画，用户感知可接受 |
| 数据库查询限制 | 云数据库单次最多返回 100 条 | 题库分页加载，无影响 |
| 并发限制 | 云开发免费环境并发有限 | MVP 用户量极小，无影响 |

---

## 八、项目目录结构（预期）

```
wx-study-helper/
├── miniprogram/                    # 小程序前端代码
│   ├── app.js                      # 全局逻辑
│   ├── app.json                    # 全局配置（页面路由、tabBar）
│   ├── app.wxss                    # 全局样式
│   ├── project.config.json         # 项目配置
│   ├── sitemap.json                # 微信索引配置
│   ├── images/                     # 图标资源
│   ├── components/                 # 自定义组件
│   │   ├── question-card/          # 题目卡片组件
│   │   ├── answer-options/         # 选项组件
│   │   └── memorize-card/          # 背诵卡片组件
│   ├── pages/                      # 页面
│   │   ├── index/                  # 首页（学习概览）
│   │   ├── practice/               # 刷题页
│   │   │   ├── index               # 选择科目/年级
│   │   │   └── quiz                # 答题中
│   │   ├── wrong-book/             # 错题本
│   │   ├── memorize/               # 背诵小屋
│   │   ├── compose/                # 作文批改（V2）
│   │   ├── profile/                # 个人中心
│   │   └── children/               # 孩子档案管理
│   └── utils/                      # 工具函数
│       ├── api.js                  # 云函数调用封装
│       ├── forget-curve.js         # 遗忘曲线算法
│       └── format.js               # 格式化工具
├── cloudfunctions/                 # 云函数
│   ├── getOpenId/                  # 获取 openid + 登录
│   ├── importQuestions/            # 批量导入题目
│   ├── getReviewList/              # 获取今日待复习错题
│   ├── correctEssay/               # 作文批改（调 LLM）
│   └── generateQuestions/          # AI 生成题目
├── data/                           # 离线数据
│   ├── questions.json              # 题库数据
│   ├── memorize-items.json         # 背诵内容数据
│   └── prompts/                    # AI 生成 Prompt 模板
├── DESIGN.md                       # 本方案文档
└── README.md                       # 项目说明
```

---

## 九、遗忘曲线算法（简化版）

```javascript
// 复习间隔：第 1/2/4/7/15 天
const REVIEW_INTERVALS = [1, 2, 4, 7, 15];

function getNextReviewTime(lastReviewTime, reviewCount) {
  const intervalDays = REVIEW_INTERVALS[Math.min(reviewCount, REVIEW_INTERVALS.length - 1)];
  const nextDate = new Date(lastReviewTime);
  nextDate.setDate(nextDate.getDate() + intervalDays);
  return nextDate;
}

// 掌握度计算
function updateMastery(currentMastery, isCorrect) {
  if (isCorrect) {
    return Math.min(100, currentMastery + 20); // 答对 +20
  } else {
    return Math.max(0, currentMastery - 30);   // 答错 -30，重置复习
  }
}
```

---

## 十、下一步

1. **确认方案**：用户审阅本方案，提出修改意见
2. **准备工作**：注册小程序账号（如果还没有）、开通云开发环境
3. **开始编码**：按阶段一 → 阶段二 → ... 顺序开发

---

*文档版本：v1.0 | 最后更新：2026-08-04*
